1. Requisitos previos
Debe tener instalado: PHP 8.2, Composer, MySQL y Laravel.

2. Instalar dependencias bash
composer install

3. Configurar el .env bash

cp .env.example .env
php artisan key:generate

Luego edita el .env con los datos de su base de datos local:


APP_NAME="El Sepulturero Juan"
APP_ENV=local
APP_KEY=base64:RXK7Aku1RHduWrFAs/fxHkXn8+7gf45hGhxF8OSWCsI=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000



DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sepulturero_juan
DB_USERNAME=root
DB_PASSWORD=



SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null




MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=farmasystem8@gmail.com
MAIL_PASSWORD="upzw rctz pufr ibmf"
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="farmasystem8@gmail.com"
MAIL_FROM_NAME="El Sepulturero Juan"








4. Ejecutar el XAMP y tenerlo Corriendo el Apache y MySQL


5. Ir a http://localhost/phpmyadmin/  y Crear la base de datos en MySQL:

CREATE DATABASE sepulturero_juan CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

6. Ejecutar migraciones y seeders (en este orden)

php artisan migrate
php artisan db:seed

El db:seed ejecuta primero RolesAndPermissionsSeeder (crea roles y permisos) y luego AdminSeeder (crea el usuario admin). Ese orden ya está definido en DatabaseSeeder.php, así que no tiene que preocuparse por eso.

7. Levantar el servidor

php artisan serve

8. Ingresar al sistema
http://localhost:8000
Usuario:    admin
Contraseña: Admin1234!

⚠️ Problemas frecuentes:
Si da error de permisos de Spatie al hacer db:seed, ejecutar:

php artisan config:clear
php artisan cache:clear
php artisan db:seed

Si las migraciones fallan por orden de foreign keys:

php artisan migrate:fresh --seed

Eso borra todo y vuelve a crear, perfecto para entorno nuevo.








Como registrar cosas (orden)


Cliente

Contrato



=========================
git add .
git commit -m "mamdas   XDDDDDD"
git push
===============


$this->authorize('clientes.ver');











$this->authorize('contratos.ver');



app/Http/Controllers/EmpleadoController.php
$this->authorize('empleados.ver');

app/Http/Controllers/UsuarioController.php
$this->authorize('usuarios.ver');

app/Http/Controllers/CementerioController.php
$this->authorize('cementerios.ver');

app/Http/Controllers/EspacioController.php
$this->authorize('espacios.ver');

app/Http/Controllers/TipoInhumacionController.php
$this->authorize('cementerios.ver');

app/Http/Controllers/MantenimientoController.php
app/Http/Controllers/InhumacionController.php
app/Http/Controllers/ContratoController.php
app/Http/Controllers/VentaController.php
app/Http/Controllers/PagoController.php
app/Http/Controllers/RolController.php


Y en las vistas, todos los 

@can('clientes.crear'), 
@can('clientes.editar'), 
@can('clientes.eliminar') 

cámbialos por 


@can('clientes.ver').